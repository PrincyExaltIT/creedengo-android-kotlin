---
name: sonarqube_creedengo_android_kotlin

services:
  sonar:
    image: sonarqube:25.12.0.117093-community
    container_name: sonar_creedengo_android_kotlin
    ports:
      - "9000:9000"
    networks:
      - sonarnet
    depends_on:
      db:
        condition: service_healthy
    environment:
      SONAR_JDBC_USERNAME: sonar
      SONAR_JDBC_PASSWORD: sonar
      SONAR_JDBC_URL: jdbc:postgresql://db:5432/sonarqube
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
    volumes:
      - type: bind
        source: ./build/libs/creedengo-android-kotlin-0.0.1-SNAPSHOT.jar
        target: /opt/sonarqube/extensions/plugins/creedengo-android-kotlin-0.0.1-SNAPSHOT.jar
      - "extensions:/opt/sonarqube/extensions"
      - "logs:/opt/sonarqube/logs"
      - "data:/opt/sonarqube/data"
    healthcheck:
      test: 'host="$(hostname -i || echo ''127.0.0.1'')" && curl -sf http://$$host:9000/api/system/status | grep -qE ''"status":"(UP|DB_MIGRATION_REQUIRED|DB_MIGRATION_RUNNING|MIGRATION_SUCCEEDED)"'''
      interval: 10s
      timeout: 10s
      retries: 5

  db:
    image: postgres:17-alpine
    container_name: postgresql_creedengo_android_kotlin
    networks:
      - sonarnet
    volumes:
      - pg_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: sonar
      POSTGRES_PASSWORD: sonar
      POSTGRES_DB: sonarqube
      PGDATA: pg_data:/var/lib/postgresql/data/pgdata
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sonar -d sonarqube"]
      interval: 5s
      timeout: 5s
      retries: 5

networks:
  sonarnet:
    driver: bridge

volumes:
  data:
  logs:
  extensions:
  pg_data:
